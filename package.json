{
	"name": "react-app-rewire-sass-rule",
	"description": "Add SASS to create-react-app project with react-app-rewired",
	"version": "2.2.1",
	"main": "index.js",
	"license": "MIT",
	"scripts": {
		"test": "echo NO_TESTS_YET",
		"postversion": "git push origin && git push origin --tags"
	},
	"repository": {
		"url": "https://github.com/bluesky66-dev/react-app-rewire-sass-rule",
		"type": "git"
	},
	"dependencies": {
		"node-sass": "^8.0.0",
		"sass-loader": "^13.2.2"
	}
}
